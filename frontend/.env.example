# EkJagah Frontend Environment Configuration
# Optional: Set this in Vercel Project Settings > Environment Variables
# If you host your backend externally (e.g. Render, Railway, Fly.io, or VPS), provide the URL here.
# If left empty, EkJagah automatically runs with built-in client fallbacks so all features work seamlessly!
VITE_API_BASE_URL=
